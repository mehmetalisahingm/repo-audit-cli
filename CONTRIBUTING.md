# Contributing

## Setup

1. Create a virtual environment if you want one.
2. Install the package locally with `python -m pip install .`.
3. Run `python -m unittest discover -s tests -v`.

## Expectations

- Keep the CLI dependency-free unless there is a strong reason not to.
- Add or update tests for behavior changes.
- Keep output formats stable when possible.
